export type TravelPlanInfoType = {
    planID: string;
    title: string;
    start: string;
    end: string;
    timeCreated: string;
}

export type UserPlanLinkType = {
    email: string;
    planID: string;
}